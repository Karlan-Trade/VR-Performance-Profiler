# 更新记录

## [1.2.0] - 2026-06-18

- 应用文件改由 MSI 内嵌自定义动作复制和按 manifest 卸载，不再进入 Windows Installer `File` 表，避免自定义盘符卸载时触发 `Config.Msi` 回滚目录权限错误。
- 自定义安装路径会规范化到 `VR Performance Profiler` 产品子目录，避免直接把文件铺到用户选择的父目录或盘符根目录。
- 安装时会排除内部载荷文件 `payload.zip`，避免它出现在最终安装目录。

## [1.1.1] - 2026-06-17

- 手腕模式偏移改为使用独立的 X/Y/Z 设置，不再复用 HUD 偏移，避免调 HUD 时影响手腕面板位置。
- 新增“手腕偏移系数”设置，默认 0.25，可在设置页调整手腕偏移滑杆的实际移动幅度。
- 保留 SteamVR 初始化探针的隔离能力，但整合进主程序的 `--steamvr-init-probe` 模式，安装包不再附带单独的 `vr_perf_vr_init_probe.exe`。
- 打包脚本会从可用的本地依赖缓存中选择 OpenVR 与 nlohmann/json，减少离线构建时的依赖拉取失败。

## [1.1.0] - 2026-06-16

- 设置页新增 HUD/手腕模式通用的 X/Y/Z 覆盖层偏移控制，并支持一键重置 HUD 大小和偏移。
- 移除旧 Win32 设置窗口，设置页改为仅使用 WebView2，并改善高 DPI 下的显示清晰度。
- 修复 SteamVR 连接时 UI 无响应、连接状态卡住，以及 OpenVR 初始化后可能跳过覆盖层创建的问题。
- 新增 SteamVR 初始化探针辅助程序，避免 SteamVR 自身无响应时拖死主程序；安装包与便携包会一并包含该辅助程序。
